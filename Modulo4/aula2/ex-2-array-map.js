// map retorna um novo array onde o item é o valor da expressão indicada, sem alterar o array target
const frutas = ['goiaba', 'melancia', 'banana'];

frutas.map((fruta, index) => console.log(`${index} - ${fruta}`));