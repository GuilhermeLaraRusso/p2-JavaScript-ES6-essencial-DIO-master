// Retorna um boolean (true ou false) verificando se determinado elemento existe no arrau

const arr = [1, 3, 4, 5];

const contemUm = arr.includes(1);
console.log(contemUm);

const contemDois = arr.includes(2);
console.log(contemDois);
